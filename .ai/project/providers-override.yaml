# .ai/project/providers-override.yaml
# Generated by /workflows:quickstart on 2026-02-10
# Only override what differs from auto-detection

execution_mode: agent-executes  # AI writes code, runs tests, auto-corrects. User reviews at checkpoints.

# Review intensity: strict
review:
  intensity: strict              # All 7 review agents active
  solid_threshold:
    proceed: 18                  # Minimum to continue working
    approve: 22                  # Minimum to approve PR

# Team context
team:
  size: small                    # Small team (2-5) — standard coordination via 50_state.md
